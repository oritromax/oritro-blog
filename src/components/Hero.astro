---
---

<section class="hero">
  <div class="hero-content">
    <h1>
      <span class="greeting">Hello, I'm John.</span>
      <span class="description">I design and build digital experiences.</span>
    </h1>
    <p>A passionate developer focused on creating intuitive and engaging user interfaces with clean, efficient code.</p>
    <div class="cta-buttons">
      <a href="#projects" class="primary-button">View My Work</a>
      <a href="#contact" class="secondary-button">Get in Touch</a>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: calc(100vh - var(--header-height));
    display: flex;
    align-items: center;
    padding: var(--spacing-5) 0;
  }

  .hero-content {
    max-width: 800px;
  }

  h1 {
    font-size: 3.5rem;
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: var(--spacing-4);
    display: flex;
    flex-direction: column;
    gap: var(--spacing-1);
  }

  .greeting {
    color: var(--color-accent);
  }

  p {
    font-size: 1.25rem;
    color: var(--color-text-light);
    margin-bottom: var(--spacing-4);
    max-width: 600px;
  }

  .cta-buttons {
    display: flex;
    gap: var(--spacing-3);
  }

  .primary-button, .secondary-button {
    padding: var(--spacing-2) var(--spacing-4);
    border-radius: var(--border-radius);
    font-weight: 500;
    transition: var(--transition-default);
  }

  .primary-button {
    background-color: var(--color-accent);
    color: white;
  }

  .primary-button:hover {
    background-color: var(--color-accent-hover);
    transform: translateY(-2px);
  }

  .secondary-button {
    background-color: transparent;
    color: var(--color-text);
    border: 1px solid var(--color-border);
  }

  .secondary-button:hover {
    border-color: var(--color-accent);
    color: var(--color-accent);
    transform: translateY(-2px);
  }

  @media (max-width: 768px) {
    h1 {
      font-size: 2.5rem;
    }

    p {
      font-size: 1.1rem;
    }
  }

  @media (max-width: 640px) {
    .cta-buttons {
      flex-direction: column;
      gap: var(--spacing-2);
      width: 100%;
    }

    .primary-button, .secondary-button {
      text-align: center;
    }

    h1 {
      font-size: 2rem;
    }
  }
</style>